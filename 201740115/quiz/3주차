1. 상수를 선언하는 const는 반복문 또는 if문 안에서 사용이 가능하다.(O,X) -> X(함수나 반복문에서 중복된 상수명을 선언할 수 있기 때문에 사용할 수 없다.)

2. 상수의 종류에는 정수형, 실수형, 문자, 문자열이 있다(O,X) -> O(상수의 종류에는 소수점 이하 부분이 없는 정수, 소수점이나 지수가 있는 실수, 하나의 단일 문자, 두 개 이상의 문자가 모인 문자열이 있습니다.)

3. 원본 저장소를 가지고 와서 병합하는 git 명령어 -> git fetch src => git merge src/master