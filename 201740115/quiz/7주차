1. echo와 print_r이 무엇을 출력하는가? -> break

2. 문자열의 개수를 세주는 함수는? -> strlen()
strlen 함수는 영문 문자열이 몇 바이트 인지 가져오는 함수이다.

3. foreach의 특징은? -> foreach문은 배열에서만 작동한다.
foreach를 통해 처음부터 끝까지 배열 값을 이용하고 존재하는 각 요소를 꺼내 쓸 수 있다.