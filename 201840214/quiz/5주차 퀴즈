<5주차>
Q1. <br>태그와 <hr>태그의 차이점은??
A1. <br>태그는 줄바꿈 태그이고, <hr>태그는 밑줄태그이다.

Q2. 문자열을 결합하는 소스코드는??
A2. echo $sum.
    -해설-> php에서 문자열 결합시 .를 사용한다.
            php의 산술 연산자의 더하기 기호는 문자 결합에 사용될 수 없다.

Q3. 변수의 메모리 주소를 표현하는 기호는 무엇일까용????
    (1) #   (2) ^   (3) &   (4) *
A3. (3) &
    -해설->  & 기호는 변수의 메모리 주소를 표현하는 기호입니다.