<6주차>
Q1. <>괄호 안의 문맥상 맞는 말을 고르시오.
    <include/require>는 파일이 존재하지 않아도 프로그램을 게속 실행한다.
A1. include
    -해설-> include는 파일이 존재하지 않으면 경고를 주고 프로그램은 계속 실행되지만
            require는 파일이 존재하지 않으면 프로그램을 중단한다.

Q2. 다음 소스코드 실행 시 출력되는 결과는??
    <?php
    $age = 20;
    if ($age>=18)
        {
        echo"성인";
        print "입니다.";
        }
    else
        {
        print"미성년";
        echo"입니다";
        }
A2. 성인입니다.
    -해설-> 20이 if 조건문 중 성인을 출력하는 값인 18 이상이기 때문에
    '성인입니다'가 출력된다.

Q3. require_once "hello.php";
    require_once "hello.php"; 를 2번 쓰면 충돌이 나는 이유는??
A3. require_once 를 두 번 읽혔기 때문이다.
    -해설->  _once 는 여러번 포함해도 1번으로 간주한다.