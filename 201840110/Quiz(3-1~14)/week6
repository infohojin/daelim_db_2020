//6주차 퀴즈

//1. 함수의 선언은 코드를 묶는 선언일뿐 실행은 되지 않는다 (O,X)
// 답 : O

//2. 선언된 함수에서 전달받는 값을 인자값이라고 한다 (O,X)
// 답 : X , 인자값이 아닌 매개변수라고 함. 함수를 호출할때 전달되는 걸 인자값이라고 함. 
// 인자값이 매개변수로 복사되어 전달됨.

//3. _once 는 여러번 포함해도 1번으로 간주한다? (O,X)
// 답 : O
