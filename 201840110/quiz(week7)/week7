// 7주차 문제

// 1. 인자값과 매개변수가 매칭이 되어야 한다(O,X)
// 답 : O. 이런걸 의존성 있다고 함

// 2. 외부의 전역변수를 사용하기 위해서 쓸 수 있는 키워드는?
// 답 : global

// 3. 코드를 작성할 때 변수의 이름은 중복 사용이 가능하다.
// 답 : X, 하지만 함수일 때, 함수 안에 있는 값에 함수 밖에 있는 값이 복사되서 넘어가는 거기 때문에 이름이 중복되어도 무관
 