1. 함수의 입력은 (    )로 출력은 (     )으로 한다.
정답 : 중괄호(), return
해설 : 함수의 입력은 중괄호(), 출력은 return 키워드를 통해 합니다.

2. 함수는 코드블럭 안에 있으면 코드 블럭 전에 선언해 주어야 출력이 된다.
정답 : O
해설 : 함수가 코드 블럭 안에 존재하면 해당 블럭 전에 선언해 주어야 한다.

3. require는 파일이 존재하지 않으면 경고를 하고,
include는 파일이 존재하지 않으면 프로그램을 중단한다 
정답 : X
해설 : 파일이 존재하지 않으면 include가 경고를 하고, 
require은 프로그램을 중단한다. 
①   ②  ③  ④ 