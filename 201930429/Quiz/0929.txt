9월 29일
1. 함수에 대한 설명으로 옳지 않은 것은? 답 : 3번
1) 함수의 이름은 중복할 수 없다.
2) php에는 내장함수가 있다.
3) 함수의 선언은 호출 전/후 모두 위치 할 수 없다.
4) 함수는 조건문 내에 위치할 수 있다.

2. 내부 함수를 찾아주는 함수는? 답 : 2번
1) function_find();
2) function_exits();
3) function_select();
4) function_connect();

3.다른 파일로 함수를 호출하는 메소드를 모두 고르시오? 답 : 1번, 2번
1) include
2) require
3) import
4) file