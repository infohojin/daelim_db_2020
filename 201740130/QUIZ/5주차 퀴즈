1. HTML에서 줄바꿈을 하는 태그는?

답: <br>

해설: "\n" 과 같은 이스케이프 문자로 줄바꿈을 할 수 있으나 php 내부에서의 줄바꿈이기에 실제
브라우저에서 줄바꿈을 하려면 <br> 태그를 사용해야 한다.

2. php웹서버를 실행했을때, 자동으로 실행되는 파일명은? 

답: index.php
해설: var_dump 함수는 배열의 키와 해당하는 값, 속성까지 표시하는 출력문이다.

3. PHP에서 문자열을 결합할 때 쓰이는 기호는?

답: .

해설: PHP 산술 연산자의 더하기 기호는 문자 결합에 사용될 수 없다.