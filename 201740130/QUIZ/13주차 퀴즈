1. 테이블의 구조문을 확인할 수 있는 쿼리는?

답 : desc
해설 : desc 테이블명; 을 통해 테이블의 구조를 확인할 수 있다.

2. for와 while의 차이는?

답 : 반복 횟수 인지 유무
해설 : 반복 횟수를 알고 있을 때 for문을 사용하고, 반복 횟수를 모를때 while문을
사용한다.

3. 공백이나 문자열을 제거하는 함수는?

답 : trim
해설 : ltrim 은 왼쪽의 문자열을, rtrim은 오른쪽의 문자열을,
trim 은 양 옆의 문자열을 지워주는 역할을 한다.