1. 서버에 값을 전송하기 위한 함수 두 가지는?

답 : get, post
해설 : 두 함수 모두 서버에 값을 전송하지만 
    get은 url로, post는 form으로 넘기는 방식의 차이가 있다.

2. 설정된 변수를 확인하는 함수는?

답 : isset
해설 : isset으로 변수가 설정되었는지 확인할 수 있다.

3. input 태그에 안내문구를 추가하는 속성은?

답 : placeholder
해설 : placeholder는 입력박스에 어떠한 값을 입력해야 하는지 알려 줄 때 쓰인다.