1. 에러가 나는 이유를 설명하시오
        $func = function() {
            echo "실행";
        }
    명령 문장이기 떄문에 세미콜론 필요

2. 외부의 전역변수를 사용하기 위해 사용하는 키워드는 무엇인가
    global

3. 실행하면 aaaaaa는 출력이 되는가?
    return $any;
    $any = "aaaaaa";
   return 에서 함수의 동작이 종료되어 출력되지 않음
