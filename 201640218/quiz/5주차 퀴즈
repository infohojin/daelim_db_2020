## 201640218 배종범

Q1. 같은 연산자와 상수를 활용하여 a,b를 출력 했지만 결과값이 다르게 나왔다. 이유를 설명하시오.  
    <?php
        $a = 9 * 9 % 10;
        $b = 10 % 9 * 9;
        echo $a;
        echo $b;
        결과 
        a : 1
        b : 9
    
    : 연산자에는 우선순위가 있는데 *,/,% 는 같은 우선순위가 있지만 보통 연산자는 좌측에서 우측으로
      처리가 된다. 따라서 a는 9 * 9 실행 후 나온 81이라는 값에 % 10 한 값이 출력되지만
      b는 10 % 9 실행후 나온 1이라는 값에 * 9 값이 출력 되기 때문에 서로 다른 값이 나오는 것이다.
   
Q2. 함수는 다른 함수를 실행 시키지 못해 각각 함수를 실행해야한다.(O/X)
    
    : X - 함수는 다른 함수를 실행할 수 있어 계층적인 구조의 함수도 실행이 가능하다.

Q3. & 기호에 대하여 간략히 서술하시오.
    
    : 변수의 메모리 주소를 표현하는 기호