## 201640218 배종범

Q1. include 와 require 의 설명으로 아닌 것을 한개 이상 고르시오? 
    1. include 와 require는 외부의 소스 코드를 읽고 처리하는 것이다.
    2. require는 include와 동일한 외부의 소스 코드를 읽고 처리하는 기능을 한다
    3. include는 파일이 없으면 프로그램 을 중단한다.
    4. require는 파일이 없으면 경고만 출력하고 계속 실행한다.

    : 3,4 
      require가 파일이 없으면 프로그램을 중단하고, include가 파일이 없으면 경고만 출력하고 계속 실행한다.

Q2. not 논리 연산자에 대하여 서술하시오.
    : 변수명 앞에 !로 사용되고 true 와 false의 값을 반대로 변경 한다. true는 false로 false는 true로 변경된다.
      

Q3. 함수의 매개변수에 초기값을 설정하더라도 매개변수의 값을 생략하면 오류가 발생한다.(O/X)    
    : X 
      매개변수의 초기값을 설정할 경우 매개변수에 입력된 값을 호출하여 함수를 사용할 수도 있고,
      생략할 경우 초기값이 호출되어 사용 된다.
        